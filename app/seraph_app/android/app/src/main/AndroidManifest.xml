<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- Ticket 22, headless sync engine
         (.scratch/gallery-mode/issues/22-headless-sync-engine.md): a
         user-initiated backup run keeps going with the screen off via a
         `dataSync` foreground service (`flutter_foreground_task`), so a
         large batch survives the app being backgrounded. Android 15 caps
         cumulative `dataSync` foreground runtime at ~6 hours/day, which is
         why unattended background scheduling (ticket 24) is WorkManager-
         based rather than a long-lived service - this permission and
         service cover only the user-initiated, foreground case. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Gallery Mode's Local Source seam (ticket 15,
         .scratch/gallery-mode/issues/15-local-source-scan-and-merged-gallery.md):
         lets the app's MediaStore scan see the device's photos. minSdk is 34,
         so READ_MEDIA_IMAGES alone covers full access - no legacy
         READ_EXTERNAL_STORAGE fallback is needed. Declaring the permission
         here does not itself prompt the user; the runtime request, with the
         explanation the user is owed, is ticket 16's job (MainActivity's
         requestPermission channel method). Until the user grants either
         permission below, MainActivity's MediaStore query simply reports no
         photos. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Android 14's partial-grant permission (ticket 16,
         .scratch/gallery-mode/issues/16-photo-permissions-and-partial-grant.md):
         granted instead of READ_MEDIA_IMAGES when the user picks "Select
         photos and videos" rather than "Allow all". Requesting it alongside
         READ_MEDIA_IMAGES is what makes the system show that choice; without
         it Android would only ever offer all-or-nothing. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>

    <application
        android:label="seraph_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:fullBackupContent="@xml/backup_rules"
        android:extractNativeLibs="true"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <meta-data android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" 
            tools:ignore="Instantiatable">

            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- ADD THIS "RECEIVER" element -->
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.intent.action.MEDIA_BUTTON" />
        </intent-filter>
        </receiver>

        <!-- Ticket 22: flutter_foreground_task's own foreground service -
             its class name is fixed by the plugin ("do not change" per its
             own docs); declaring it here (rather than only in the plugin's
             manifest, which does not declare it at all - see the plugin's
             `android/src/main/AndroidManifest.xml`) is what actually gives
             it the `dataSync` type, matching the permission above. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
